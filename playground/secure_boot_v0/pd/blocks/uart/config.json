{
  "DESIGN_NAME": "uart",
  "CLOCK_PORT": "clk_i",
  "CLOCK_PERIOD": 100,
  "PNR_SDC_FILE": "dir::uart.sdc",
  "SIGNOFF_SDC_FILE": "dir::uart.sdc",
  "PDK": "gf180mcuD",

  "FP_SIZING": "absolute",
  "DIE_AREA": [0, 0, 640, 580],
  "PL_TARGET_DENSITY_PCT": 55,
  "//halo": "Default M4/M5 PDN + core ring for the halo (channel) top integration. Die grown 565x500->640x580 and pins distributed across all 4 edges (was all-West): the West ring segment + all-West pins at 80% util caused GRT-0116 (2282 overflow, M3-heavy). Spreading pins + ~0.52 util clears it. Ring gives the top a perimeter to tie onto in the channels.",
  "IO_PIN_ORDER_CFG": "dir::pin_order.cfg",
  "STA_CORNERS": ["nom_tt_025C_5v00"],
  "SYNTH_LATCH_MAP": "dir::latch_map.v",
  "PDN_CORE_RING": true,

  "//": "gf180mcuD @ 10 MHz. OpenTitan UART. Single-sided I/O: all pins on the West edge (pin_order.cfg = '#W' + '.*'). Height pinned at 500um; width widened to 565um so core util ~0.75 (UART std-cell ~193k um2) -- enough headroom for CTS/legalization with all I/O on one edge. (500x500 = ~85% util failed CTS detailed placement.) VERILOG_FILES/USE_SLANG injected by harden_block.sh."
}
