{
  "DESIGN_NAME": "xbar_tlul_2to4",
  "CLOCK_PORT": "clk_i",
  "CLOCK_PERIOD": 50,
  "PNR_SDC_FILE": "dir::xbar.sdc",
  "SIGNOFF_SDC_FILE": "dir::xbar.sdc",
  "PDK": "gf180mcuD",

  "FP_SIZING": "absolute",
  "DIE_AREA": [0, 0, 560, 560],
  "PL_TARGET_DENSITY_PCT": 25,
  "IO_PIN_ORDER_CFG": "dir::pin_order.cfg",

  "//": "VERILOG_FILES, VERILOG_INCLUDE_DIRS, USE_SLANG and SLANG_ARGUMENTS are injected by harden_block.sh from the FuseSoC-resolved closure (raw SystemVerilog read via the bundled slang frontend; no sv2v). Pad-limited block: the 2-host x 4-device TL-UL struct interface flattens to ~1056 pins, so the die is sized by pin perimeter (400um/side), not core logic. Derisk pass: relaxed 20ns clock."
}
